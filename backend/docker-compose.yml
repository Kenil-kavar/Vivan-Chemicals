version: '3.8'

services:
  app:
    build: .
    restart: always
    ports:
      - "8001:8000"
    environment:
      - PYTHONPATH=/app
    volumes:
      - .:/app
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
